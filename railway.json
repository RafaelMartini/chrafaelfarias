{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "rm -rf dist .nitro .tanstack node_modules/.vite; rm -rf node_modules/.cache/* 2>/dev/null; NITRO_PRESET=node-server npm run build"
  },
  "deploy": {
    "startCommand": "node dist/server/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
